name: Vuln Bank - DevSecOps CI

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  schedule:
    - cron: "0 18 * * *" # ~01:00 WIB
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  security-events: write

env:
  REPORT_DIR: security-reports
  PYTHON_BIN: python3.11
  EPSS_THRESHOLD: "0.5"
  DAST_URL: http://vuln-bank-dast:5000
  ZAP_FAIL_SEVERITY: HIGH
  ZAP_DEFECTDOJO_REPORT: security-reports/zap/zap_defectdojo.json

# =====================================================
# 0️⃣ CREATE DEFECTDOJO ENGAGEMENT (PR-AWARE)
# =====================================================
jobs:
  create_engagement:
    name: Create DefectDojo Engagement
    if: github.event_name == 'pull_request'
    runs-on: self-hosted

    outputs:
      engagement_id: ${{ steps.export.outputs.engagement_id }}

    env:
      DEFECTDOJO_URL: ${{ secrets.DEFECTDOJO_URL }}
      DEFECTDOJO_API_KEY: ${{ secrets.DEFECTDOJO_API_KEY }}
      DEFECTDOJO_PRODUCT_ID: ${{ secrets.DEFECTDOJO_PRODUCT_ID }}

    steps:
      - uses: actions/checkout@v4

      - name: Create or reuse engagement
        run: |
          chmod +x scripts/create_defectdojo_engagement.sh
          bash scripts/create_defectdojo_engagement.sh

      - name: Export engagement ID
        id: export
        run: |
          echo "engagement_id=${DEFECTDOJO_ENGAGEMENT_ID:-}" >> "$GITHUB_OUTPUT"

# =====================================================
# 1️⃣ CONTINUOUS SECURITY ASSURANCE
# =====================================================
  security_scans:
    name: Continuous Security Assurance
    runs-on: self-hosted
    needs: create_engagement

    outputs:
      gate_failed: ${{ steps.gate_decision.outputs.failed }}

    steps:
      # -------------------------------------------------
      # Checkout
      # -------------------------------------------------
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          clean: false

      - name: Prepare directories
        run: |
          mkdir -p $REPORT_DIR/zap
          mkdir -p $REPORT_DIR/bandit
          mkdir -p .trivy-cache

      # -------------------------------------------------
      # IaC – Checkov 
      # -------------------------------------------------
      - name: IaC Scan (Checkov)
        run: |
          docker run --rm \
            -u $(id -u):$(id -g) \
            -v "$PWD:/repo" \
            bridgecrew/checkov:latest \
            -d /repo \
            --framework dockerfile,github_actions \
            -o json \
            > "$REPORT_DIR/checkov.json" || true

      # -------------------------------------------------
      # Secrets – Gitleaks
      # -------------------------------------------------
      - name: Secret Scan (Gitleaks)
        run: |
          docker run --rm \
            -v "$PWD:/repo" \
            zricethezav/gitleaks:latest \
            detect \
              --source /repo \
              --config /repo/.gitleaks.toml \
              --report-format sarif \
              --report-path /repo/$REPORT_DIR/gitleaks.sarif \
              --redact \
              --exit-code 0

      - name: Upload Gitleaks SARIF
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: security-reports/gitleaks.sarif

      - name: Gitleaks Severity Gate
        run: |
          $PYTHON_BIN scripts/gitleaks_gate.py \
            $REPORT_DIR/gitleaks.sarif

      # -------------------------------------------------
      # SAST – Scan
      # -------------------------------------------------
      - name: SAST Scan (Semgrep – OWASP/ASVS)
        run: |
          docker run --rm \
            -v "$PWD:/src" \
            returntocorp/semgrep \
            semgrep scan \
              --config p/owasp-top-ten \
              --config p/security-audit \
              --config p/python \
              --json \
              --output $REPORT_DIR/semgrep.json \
              --error

      - name: Semgrep OWASP Gate
        run: |
          python3 scripts/semgrep_owasp_gate.py \
            $REPORT_DIR/semgrep.json

      - name: ASVS Coverage Report
        run: |
          python3 scripts/asvs_coverage.py \
            $REPORT_DIR/semgrep.json \
            $REPORT_DIR/bandit_dd.json \
          | tee $REPORT_DIR/asvs-coverage.txt

      - name: SAST Scan (Bandit)
        run: |
          $PYTHON_BIN -m pip install -q bandit
          bandit -r app.py database.py auth.py ai_agent_deepseek.py \
            -f json -o "$REPORT_DIR/bandit.json" || true

      - name: Merge SAST → Sonar
        run: |
          $PYTHON_BIN scripts/merge_security_to_sonar.py

      - name: Bandit (DefectDojo)
        run: |
          $PYTHON_BIN scripts/bandit_normalize.py \
            "$REPORT_DIR/bandit.json" \
            "$REPORT_DIR/bandit_dd.json"

      - name: SonarQube Scan
        uses: sonarsource/sonarqube-scan-action@v2
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}

      # -------------------------------------------------
      # SCA – Trivy FS (non-root)
      # -------------------------------------------------
      - name: SCA Scan (Trivy)
        run: |
          docker run --rm \
            -u $(id -u):$(id -g) \
            -v "$PWD:/work" \
            -v "$PWD/.trivy-cache:/tmp/trivy-cache" \
            aquasec/trivy:0.49.1 \
            fs /work \
            --severity HIGH,CRITICAL \
            --ignore-unfixed \
            --cache-dir /tmp/trivy-cache \
            --format json \
            -o /work/$REPORT_DIR/trivy-sca.json

      # -------------------------------------------------
      # DAST – OWASP ZAP (Automation Framework)
      # -------------------------------------------------
      - name: Build Application Image
        run: |
          docker build \
            -f containers/app/Dockerfile \
            -t vuln-bank:ci .

      - name: Start DAST Stack
        run: |
          docker compose -f docker-compose.dast.yml up -d

      - name: Wait for App Readiness
        run: |
          for i in {1..20}; do
            docker run --rm \
              --network vuln-bank_dastnet \
              curlimages/curl:8.6.0 \
              curl -fs "$DAST_URL" && exit 0
            sleep 5
          done
          docker compose -f docker-compose.dast.yml logs
          exit 1

      - name: ZAP Automation Scan
        run: |
          chmod -R 777 $REPORT_DIR/zap

          docker run --rm \
            --network vuln-bank_dastnet \
            -v "$PWD/$REPORT_DIR/zap:/zap/wrk" \
            -v "$PWD/security-policies/zap-automation.yml:/zap/automation.yml:ro" \
            -e target_url="$DAST_URL" \
            ghcr.io/zaproxy/zaproxy:stable \
            zap.sh -cmd -autorun /zap/automation.yml

          test -f $REPORT_DIR/zap/zap_alerts.json

      - name: ZAP Severity Gate
        run: |
          $PYTHON_BIN scripts/zap_severity_gate.py \
            $REPORT_DIR/zap/zap_alerts.json \
            $ZAP_FAIL_SEVERITY

      - name: Stop DAST Stack
        if: always()
        run: docker compose -f docker-compose.dast.yml down || true

      # -------------------------------------------------
      # EPSS + CISA KEV Gate
      # -------------------------------------------------
      - name: EPSS + CISA KEV Gate
        run: |
          $PYTHON_BIN -m pip install -q requests
          $PYTHON_BIN scripts/epss_gate.py \
            --input "$REPORT_DIR/trivy-sca.json" \
            --output "$REPORT_DIR/epss-findings.json" \
            --threshold "$EPSS_THRESHOLD"

      # -------------------------------------------------
      # Final Gate Decision
      # -------------------------------------------------
      - name: Gate Decision
        id: gate_decision
        run: |
          if [ -f "$REPORT_DIR/gate_failed" ]; then
            echo "failed=true" >> "$GITHUB_OUTPUT"
            exit 1
          else
            echo "failed=false" >> "$GITHUB_OUTPUT"
          fi

      # -------------------------------------------------
      # Artifacts (ONE TIME)
      # -------------------------------------------------
      - name: Upload Security Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: security-reports
          path: security-reports

# =====================================================
# 2️⃣ IMPORT RESULTS → DEFECTDOJO
# =====================================================
  import_defectdojo:
    name: Import Security Reports → DefectDojo
    needs:
      - security_scans
      - create_engagement
    if: always()
    runs-on: self-hosted

    env:
      DEFECTDOJO_URL: ${{ secrets.DEFECTDOJO_URL }}
      DEFECTDOJO_API_KEY: ${{ secrets.DEFECTDOJO_API_KEY }}
      DEFECTDOJO_PRODUCT_ID: ${{ secrets.DEFECTDOJO_PRODUCT_ID }}
      DEFECTDOJO_ENGAGEMENT_ID: ${{ needs.create_engagement.outputs.engagement_id || secrets.DEFECTDOJO_ENGAGEMENT_ID }}

    steps:
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          name: security-reports
          path: security-reports

      - name: Import all findings to DefectDojo
        run: |
          chmod +x scripts/import_defectdojo.sh
          bash scripts/import_defectdojo.sh

  pr_security_comment:
    name: PR Comment – DefectDojo Summary
    needs:
      - create_engagement
      - import_defectdojo
    if: github.event_name == 'pull_request'
    runs-on: self-hosted
    continue-on-error: true

    env:
      DEFECTDOJO_URL: ${{ secrets.DEFECTDOJO_URL }}
      DEFECTDOJO_API_KEY: ${{ secrets.DEFECTDOJO_API_KEY }}
      DEFECTDOJO_ENGAGEMENT_ID: ${{ needs.create_engagement.outputs.engagement_id }}
      PR_NUMBER: ${{ github.event.pull_request.number }}

    steps:
      - uses: actions/checkout@v4

      - name: Post PR security summary
        run: |
          python3 scripts/defectdojo_pr_summary.py
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  close_engagement:
    name: Close DefectDojo Engagement (PR merged)
    needs:
      - create_engagement
      - import_defectdojo
    runs-on: self-hosted
    if: >
      github.event_name == 'pull_request' &&
      github.event.pull_request.merged == true

    env:
      DEFECTDOJO_URL: ${{ secrets.DEFECTDOJO_URL }}
      DEFECTDOJO_API_KEY: ${{ secrets.DEFECTDOJO_API_KEY }}
      DEFECTDOJO_ENGAGEMENT_ID: ${{ needs.create_engagement.outputs.engagement_id }}

    steps:
      - name: Close engagement in DefectDojo
        run: |
          if [ -z "$DEFECTDOJO_ENGAGEMENT_ID" ]; then
            echo "[SKIP] No engagement ID"
            exit 0
          fi

          curl -sS -X PATCH \
            "$DEFECTDOJO_URL/api/v2/engagements/$DEFECTDOJO_ENGAGEMENT_ID/" \
            -H "Authorization: Token $DEFECTDOJO_API_KEY" \
            -H "Content-Type: application/json" \
            -d '{"status":"Completed"}'

          echo "[OK] Engagement closed"

# =====================================================
# 3️⃣ ZAP ALERTS → GITHUB ISSUES
# =====================================================
  zap_create_issues:
    name: ZAP Alerts → GitHub Issues
    needs: security_scans
    if: always()
    runs-on: ubuntu-latest

    steps:
      # ✅ REQUIRED: bring scripts into workspace
      - uses: actions/checkout@v4

      - uses: actions/download-artifact@v4
        with:
          name: security-reports
          path: security-reports

      - name: Create GitHub Issues from ZAP alerts
        run: |
          python3 scripts/zap_to_github_issues.py \
            security-reports/zap/zap_alerts.json
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_REPOSITORY: ${{ github.repository }}

# =====================================================
# SLACK NOTIFICATION
# =====================================================
  slack_notify:
    name: Slack Alert (Gate Failed)
    needs: security_scans
    if: needs.security_scans.outputs.gate_failed == 'true'
    runs-on: self-hosted

    steps:
      - name: Send Slack Alert
        run: |
          curl -X POST -H "Content-Type: application/json" \
            --data "{
              \"text\": \"🚨 *Vuln Bank Security Gate FAILED*\\nRepo: ${{ github.repository }}\\nRun: ${{ github.run_id }}\"
            }" \
            "${{ secrets.SLACK_WEBHOOK_URL }}"
