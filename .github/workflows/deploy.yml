name: Vuln Bank DevSecOps Pipeline (v7.3 UltraStable Auto-Detect)

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  schedule:
    - cron: "0 18 * * *" # 01:00 WIB daily
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  security-events: write
  id-token: write

concurrency:
  group: vuln-bank-${{ github.ref }}
  cancel-in-progress: true

env:
  APP_NAME: vuln-bank
  REPORT_DIR: security-reports
  PYTHON_BIN: python3.11
  EPSS_THRESHOLD: "0.5"
  POLICY_DIR: policy
  IMAGE_REPO: xbash81/vuln-bank
  IMAGE_TAG: ${{ github.sha }}
  KYVERNO_ENABLED: "false"

jobs:

# =====================================================================
# 0) DETERMINE SCAN MODE
# =====================================================================
  prep:
    runs-on: self-hosted
    outputs:
      scan_mode: ${{ steps.mode.outputs.mode }}
    steps:
      - id: mode
        run: |
          if [[ "$GITHUB_REF" == "refs/heads/main" || "$GITHUB_EVENT_NAME" == "schedule" ]]; then
            echo "mode=full" >> $GITHUB_OUTPUT
          else
            echo "mode=light" >> $GITHUB_OUTPUT
          fi

# =====================================================================
# 1) SECURITY SCANS
# =====================================================================
  security_scans:
    runs-on: self-hosted
    needs: prep
    continue-on-error: ${{ github.event_name == 'pull_request' }}
    outputs:
      critical_found: ${{ steps.flag.outputs.critical_found }}
      scan_mode: ${{ steps.scanmode.outputs.mode }}

    steps:
      - uses: actions/checkout@v4

      # --------------------------------------------
      # Set Scan Mode
      # --------------------------------------------
      - id: scanmode
        run: |
          M="${{ needs.prep.outputs.scan_mode }}"
          echo "mode=$M" >> $GITHUB_OUTPUT
          echo "SCAN_MODE=$M" >> $GITHUB_ENV

      # --------------------------------------------
      # Cache JIT Tools
      # --------------------------------------------
      - uses: actions/cache@v4
        with:
          path: |
            .venv
            ~/.cache/pip
            ~/.cache/trivy
            ~/.cache/snyk
          key: ${{ runner.os }}-security-v7-${{ hashFiles('requirements.txt') }}

      # =====================================================================
      # INSTALL BASE TOOLS
      # =====================================================================
      - name: Setup Python + pip-audit + jq
        run: |
          $PYTHON_BIN -m venv .venv
          source .venv/bin/activate
          pip install --upgrade pip pip-audit==2.7.2

          BIN=$RUNNER_TEMP/bin
          mkdir -p "$BIN"
          echo "$BIN" >> $GITHUB_PATH

          if ! command -v jq >/dev/null; then
            curl -L -o "$BIN/jq" https://github.com/stedolan/jq/releases/download/jq-1.6/jq-linux64
            chmod +x "$BIN/jq"
          fi

      # =====================================================================
      # INSTALL SECURITY TOOLS (AUTO-DETECT OS/ARCH v7.3)
      # =====================================================================
      - name: Install Tools (Auto-Detect OS & ARCH)
        id: tools
        run: |
          set -euo pipefail

          OS=$(uname -s | tr '[:upper:]' '[:lower:]')
          ARCH=$(uname -m)

          case "$ARCH" in
            x86_64) ARCH="amd64" ;;
            aarch64) ARCH="arm64" ;;
          esac

          echo "Detected OS=$OS ARCH=$ARCH"

          BIN="$RUNNER_TEMP/bin"
          mkdir -p "$BIN"
          echo "$BIN" >> "$GITHUB_PATH"

          safe_download() {
            url="$1"
            outfile="$2"
            echo "⬇️ Downloading: $url"
            for i in {1..5}; do
              curl -sSL "$url" -o "$outfile" && break
              echo "Retry $i..."
              sleep 2
            done
            if [ ! -s "$outfile" ]; then
              echo "❌ Download failed: $url"
              exit 1
            fi
          }

          # -------------------------
          # Conftest Auto-Detect
          # -------------------------
          if ! command -v conftest >/dev/null; then
            echo "Installing Conftest..."

            VER=$(curl -s https://api.github.com/repos/open-policy-agent/conftest/releases/latest | jq -r .tag_name)

            ASSET=$(curl -s https://api.github.com/repos/open-policy-agent/conftest/releases/latest \
                    | jq -r ".assets[] | select(.name | test(\"${OS}_${ARCH}\")) | .browser_download_url" \
                    | head -n1)

            safe_download "$ASSET" conftest.tar.gz

            if ! file conftest.tar.gz | grep -q gzip; then
              echo "❌ Invalid Conftest file (not gzip)"
              exit 1
            fi

            tar -xzf conftest.tar.gz -C "$BIN"
            chmod +x "$BIN/conftest"
          fi

          # -------------------------
          # Trivy
          # -------------------------
          if ! command -v trivy >/dev/null; then
            safe_download https://github.com/aquasecurity/trivy/releases/latest/download/trivy_${OS}_${ARCH}.tar.gz trivy.tar.gz
            tar -xzf trivy.tar.gz -C "$BIN" trivy
            chmod +x "$BIN/trivy"
          fi

          # -------------------------
          # Syft
          # -------------------------
          if ! command -v syft >/dev/null; then
            safe_download https://github.com/anchore/syft/releases/latest/download/syft_${OS}_${ARCH}.tar.gz syft.tar.gz
            tar -xzf syft.tar.gz -C "$BIN" syft
            chmod +x "$BIN/syft"
          fi

          # -------------------------
          # Gitleaks
          # -------------------------
          if ! command -v gitleaks >/dev/null; then
            VER=$(curl -s https://api.github.com/repos/gitleaks/gitleaks/releases/latest | jq -r .tag_name)
            safe_download https://github.com/gitleaks/gitleaks/releases/download/${VER}/gitleaks_${VER#v}_${OS}_${ARCH}.tar.gz gitleaks.tar.gz
            tar -xzf gitleaks.tar.gz -C "$BIN"
            chmod +x "$BIN/gitleaks"
          fi

          # -------------------------
          # Snyk
          # -------------------------
          if ! command -v snyk >/dev/null; then
            safe_download https://github.com/snyk/cli/releases/latest/download/snyk-${OS}-${ARCH} snyk
            mv snyk "$BIN/snyk"
            chmod +x "$BIN/snyk"
          fi

      # =====================================================================
      # PREPARE REPORT DIRS
      # =====================================================================
      - run: |
          mkdir -p ${REPORT_DIR}/{secrets,sca,sast,misconfig,dast,sbom}
          printf "false" > critical_flag.txt

      # =====================================================================
      # SAST — SEMGREP ULTRASTABLE
      # =====================================================================
      - name: Semgrep JSON (UltraStable)
        run: |
          TEMP=$(mktemp -d)
          docker run --rm \
            -v "$GITHUB_WORKSPACE":/src \
            -v "$TEMP":/out \
            returntocorp/semgrep:latest \
            semgrep ci \
              --config p/ci \
              --json \
              --no-error \
              --force-run \
              --output /out/semgrep.json || true

          [[ ! -f "$TEMP/semgrep.json" ]] && echo "{}" > "$TEMP/semgrep.json"
          cp "$TEMP/semgrep.json" "${REPORT_DIR}/sast/semgrep.json"

          jq '[.results[]?|select(.extra.severity=="ERROR" or .extra.severity=="HIGH")] | length' \
            "${REPORT_DIR}/sast/semgrep.json" | grep -qv 0 \
            && printf "true" > critical_flag.txt || true

      - name: Semgrep SARIF
        run: |
          docker run --rm \
            -v "$GITHUB_WORKSPACE":/src \
            -v "$GITHUB_WORKSPACE/${REPORT_DIR}/sast":/out \
            returntocorp/semgrep:latest \
            semgrep ci --config p/ci --sarif --output /out/semgrep.sarif || true

      # =====================================================================
      # SECRET SCAN — GITLEAKS
      # =====================================================================
      - name: Secret Scan (Gitleaks)
        run: |
          gitleaks detect --source . \
            --report-format json \
            --report-path "${REPORT_DIR}/secrets/gitleaks.json" || true

          jq -e 'length>0' "${REPORT_DIR}/secrets/gitleaks.json" \
            && printf "true" > critical_flag.txt || true

      # =====================================================================
      # SCA — pip-audit, Trivy FS, Snyk
      # =====================================================================
      - name: pip-audit (FULL only)
        if: env.SCAN_MODE == 'full'
        run: |
          [ -f requirements.txt ] && .venv/bin/pip-audit -r requirements.txt \
            -f json -o "${REPORT_DIR}/sca/pip-audit.json" || true

      - name: Trivy FS
        run: |
          trivy fs --scanners vuln \
            --severity HIGH,CRITICAL \
            --format json \
            --output "${REPORT_DIR}/sca/trivy-fs.json" .

          jq '[.Results[].Vulnerabilities[]?|select(.Severity=="HIGH" or .Severity=="CRITICAL")] | length' \
            "${REPORT_DIR}/sca/trivy-fs.json" | grep -qv 0 \
            && printf "true" > critical_flag.txt || true

      - name: Snyk SCA
        env:
          SNYK_TOKEN: ${{ secrets.SNYK_TOKEN }}
        run: |
          snyk test --all-projects --json > "${REPORT_DIR}/sca/snyk.json" || true
          grep -qi '"critical"\|"high"' "${REPORT_DIR}/sca/snyk.json" \
            && printf "true" > critical_flag.txt || true

      # =====================================================================
      # EPSS FILTERING
      # =====================================================================
      - name: EPSS Filtering
        run: |
          jq -r '.. | .VulnerabilityID? // empty' "${REPORT_DIR}/sca/trivy-fs.json" > cves.txt || true
          jq -r '.. | .identifiers?.CVE[]? // empty' "${REPORT_DIR}/sca/snyk.json" >> cves.txt || true
          sort -u cves.txt -o cves.txt

          if [ -s cves.txt ]; then
            cat cves.txt | xargs -I {} -P 12 sh -c '
              epss=$(curl -s "https://api.first.org/data/v1/epss?cve=$1" | jq -r ".data[0].epss // 0")
              awk "BEGIN{exit !($epss > ${EPSS_THRESHOLD})}"
            ' _ {} && printf "true" > critical_flag.txt || true
          fi

      # =====================================================================
      # TRIVY CONFIG (MISCONFIG)
      # =====================================================================
      - name: Trivy Config JSON
        run: |
          trivy config --severity HIGH,CRITICAL \
            --format json \
            --output "${REPORT_DIR}/misconfig/trivy-config.json" .

          jq '[.Results[].Misconfigurations[]?|select(.Severity=="HIGH" or .Severity=="CRITICAL")] | length' \
            "${REPORT_DIR}/misconfig/trivy-config.json" | grep -qv 0 \
            && printf "true" > critical_flag.txt || true

      - name: Trivy Config SARIF
        run: |
          trivy config --severity HIGH,CRITICAL \
            --format sarif \
            --output "${REPORT_DIR}/misconfig/trivy-config.sarif" .

      # =====================================================================
      # POLICY-AS-CODE — OPA CONTFEST
      # =====================================================================
      - name: Conftest Policy Check
        run: |
          FAILED=0
          for target in Dockerfile k8s docker-compose*.yml; do
            if [ -e "$target" ]; then
              conftest test "$target" --policy "$POLICY_DIR" || FAILED=1
            fi
          done
          [[ $FAILED -eq 1 ]] && printf "true" > critical_flag.txt

      # =====================================================================
      # GENERATE SBOM
      # =====================================================================
      - name: SBOM
        run: syft . -o json > "${REPORT_DIR}/sbom/sbom.json"

      # =====================================================================
      # UPLOAD REPORT ARTIFACT
      # =====================================================================
      - uses: actions/upload-artifact@v5
        with:
          name: security-reports
          path: ${{ env.REPORT_DIR }}

      - id: flag
        run: echo "critical_found=$(cat critical_flag.txt)" >> $GITHUB_OUTPUT

# =====================================================================
# 2) UPLOAD SARIF
# =====================================================================
  upload_sarif:
    runs-on: self-hosted
    needs: security_scans
    if: always()
    steps:
      - uses: actions/download-artifact@v5
        with:
          name: security-reports
          path: security-reports
          merge-multiple: false
          retries: 8

      - name: Upload Semgrep SARIF
        if: hashFiles('security-reports/sast/semgrep.sarif') != ''
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: security-reports/sast/semgrep.sarif

      - name: Upload Trivy Config SARIF
        if: hashFiles('security-reports/misconfig/trivy-config.sarif') != ''
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: security-reports/misconfig/trivy-config.sarif

# =====================================================================
# 3) AUTO ISSUE
# =====================================================================
  create_issue:
    runs-on: self-hosted
    needs: security_scans
    if: needs.security_scans.outputs.critical_found == 'true' && github.ref == 'refs/heads/main'
    permissions:
      issues: write

    steps:
      - run: |
          cat <<EOF > issue.md
          🚨 High/Critical Security Vulnerabilities Detected

          Mode: ${{ needs.security_scans.outputs.scan_mode }}
          Commit: ${{ github.sha }}

          Please review artifact: security-reports
          EOF

      - uses: actions/github-script@v8
        with:
          script: |
            const fs = require("fs");
            const body = fs.readFileSync("issue.md", "utf8");
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: "🚨 Security Findings Detected",
              body,
              labels: ["security", "critical"]
            })

# =====================================================================
# 4) AI REMEDIATION
# =====================================================================
  ai_remediation:
    runs-on: self-hosted
    needs: security_scans
    if: needs.security_scans.outputs.critical_found == 'true'
    env:
      HAS_OPENAI: ${{ secrets.OPENAI_API_KEY != '' }}

    steps:
      - name: Skip if no key
        if: env.HAS_OPENAI == 'false'
        run: echo "No OPENAI_API_KEY, skipping AI remediation."

      - uses: actions/download-artifact@v5
        with:
          name: security-reports
          path: security-reports

      - run: |
          echo "## Security Summary" > summary.md
          echo "### Gitleaks" >> summary.md
          jq 'length' security-reports/secrets/gitleaks.json >> summary.md || echo 0 >> summary.md

      - name: Call OpenAI
        if: env.HAS_OPENAI == 'true'
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          BODY=$(jq -Rs '.' summary.md)
          curl -s https://api.openai.com/v1/chat/completions \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer $OPENAI_API_KEY" \
            -d "{
                  \"model\": \"gpt-4.1-mini\",
                  \"messages\": [
                    {\"role\": \"system\", \"content\": \"You are a senior DevSecOps engineer. Provide remediation steps.\"},
                    {\"role\": \"user\", \"content\": $BODY}
                  ]
                }" \
            | jq -r '.choices[0].message.content' > remediation.md

      - uses: actions/github-script@v8
        if: env.HAS_OPENAI == 'true'
        with:
          script: |
            const fs = require("fs");
            const body = fs.readFileSync("remediation.md", "utf8");
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: "🤖 AI Remediation Suggestions",
              body,
              labels: ["security","ai"]
            })

# =====================================================================
# 5) SECURITY GATE
# =====================================================================
  security_gate:
    runs-on: self-hosted
    needs: security_scans
    if: github.ref == 'refs/heads/main'
    steps:
      - run: |
          if [ "${{ needs.security_scans.outputs.critical_found }}" = "true" ]; then
            echo "❌ Security Gate Failed"
            exit 1
          else
            echo "✅ Security Gate Passed"
          fi

# =====================================================================
# 6) BUILD + SIGN + PUSH
# =====================================================================
  build_and_push:
    runs-on: self-hosted
    needs: security_gate
    if: needs.security_gate.result == 'success'
    steps:
      - uses: actions/checkout@v4

      - name: Docker Login
        run: echo "${{ secrets.DOCKERHUB_TOKEN }}" | docker login \
              -u "${{ secrets.DOCKERHUB_USERNAME }}" --password-stdin

      - run: docker build -t "$IMAGE_REPO:$IMAGE_TAG" .
      - run: docker push "$IMAGE_REPO:$IMAGE_TAG"

      - uses: sigstore/cosign-installer@v3

      - env:
          COSIGN_EXPERIMENTAL: "true"
        run: |
          cosign sign --yes "$IMAGE_REPO:$IMAGE_TAG"

# =====================================================================
# 7) DEPLOY APP
# =====================================================================
  deploy:
    runs-on: self-hosted
    needs: build_and_push
    if: github.ref == 'refs/heads/main'
    steps:
      - name: Deploy to VM
        env:
          REMOTE_HOST: hosting@192.168.107.135
        run: |
          ssh -o StrictHostKeyChecking=no "$REMOTE_HOST" bash -s <<EOF
            docker pull ${IMAGE_REPO}:${IMAGE_TAG}
            cd /opt/vuln-bank-app/containers/app
            IMAGE_WITH_TAG=${IMAGE_REPO}:${IMAGE_TAG} docker compose down
            IMAGE_WITH_TAG=${IMAGE_REPO}:${IMAGE_TAG} docker compose up -d
          EOF

# =====================================================================
# 8) SLACK NOTIFY
# =====================================================================
  slack_notify:
    runs-on: self-hosted
    needs: security_scans
    if: always()
    steps:
      - run: |
          [ -z "${{ secrets.SLACK_WEBHOOK_URL }}" ] && exit 0

          STATUS="✔ Pipeline success"
          [ "${{ needs.security_scans.outputs.critical_found }}" = "true" ] && STATUS="🚨 Security issues detected"

          jq -n --arg text "$STATUS" '{text:$text}' \
            | curl -X POST -H "Content-type: application/json" \
              --data @- "${{ secrets.SLACK_WEBHOOK_URL }}"
