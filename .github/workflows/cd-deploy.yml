name: DevSecOps Vuln Bank — CD Deploy

on:
  workflow_dispatch:
    inputs:
      image_tag:
        description: "Image tag to deploy"
        required: true
        default: latest

permissions:
  contents: read
  id-token: write

jobs:
  deploy:
    uses: YOUR_GH_USER/YOUR_REPO/.github/workflows/jobs/deploy.yml@main
    with:
      image_tag: ${{ github.event.inputs.image_tag }}
    secrets: inherit
