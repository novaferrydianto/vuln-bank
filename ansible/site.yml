---
# 🔧 1. (Optional) Setup bastion tools, runner, dll
- import_playbook: playbooks/setup_bastion.yml

# 🗄️ 2. Setup DB node - PostgreSQL 16 + DB vulnbank
- import_playbook: playbooks/setup_db.yml

# 🖥️ 3. Setup hosting node - Docker + Docker Compose
- import_playbook: playbooks/setup_hosting.yml

# 🛡️ 4. Hardening hosting node (CIS + OpenSCAP scan)
- import_playbook: playbooks/hardening_hosting.yml

# ✅ 5. Verifikasi laporan OpenSCAP (cek status / copy report, dll)
- import_playbook: playbooks/verify_openscap.yml

# 🚀 6. Deploy aplikasi Vuln Bank (pull code / rsync + docker compose up)
- import_playbook: playbooks/deploy_app.yml
