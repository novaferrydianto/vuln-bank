---
- name: Verify OpenSCAP (placeholder for demo)
  hosts: hosting
  become: true

  vars:
    cis_min_score: "{{ cis_min_score | default(70) }}"

  tasks:
    - name: Placeholder OpenSCAP check
      ansible.builtin.debug:
        msg: "OpenSCAP verification would run here. Min score: {{ cis_min_score }}"
